// math constants
const rad2deg = 180/Math.PI
const deg2rad = Math.PI/180
const PI = Math.PI
const sin = Math.sin
const cos = Math.cos
const abs = Math.abs
const pow = Math.pow
function rad(x:number){return x * deg2rad}
function deg(x:number){return x * rad2deg}
